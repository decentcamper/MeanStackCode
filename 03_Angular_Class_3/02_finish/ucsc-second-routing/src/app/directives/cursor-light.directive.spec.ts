import { CursorLightDirective } from './cursor-light.directive';

describe('CursorLightDirective', () => {
  it('should create an instance', () => {
    const directive = new CursorLightDirective();
    expect(directive).toBeTruthy();
  });
});
